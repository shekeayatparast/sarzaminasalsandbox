- C18 (Agent sidebar active state): The `isActive` function used `pathname.startsWith(href + "/")` which caused `/agent/orders` to be "active" when on `/agent/orders/new`. Fixed by special-casing `/agent/orders` to exact-match only.
- C19 (City auto-load): Discovered the city was NOT loading from the agent profile despite `setCity(profileData.agent.city)` being called. Root cause: Radix Select has an internal mechanism that calls `onValueChange("")` when the items list changes (from `[]` to the city list), which reset the just-set city. Fix: moved city initialization to a separate `useEffect` with a `setTimeout(0)` delay so Radix settles its items before the city value is set. Also removed the old `useEffect` that cleared city on province change (replaced with an explicit `handleProvinceChange` handler on the province Select). Verified: province + city both load correctly now.
- Schema changes (B10/B14 prerequisites): Added `stockKg Float @default(0)` and `agentPricePerKg Int @default(0)` to the Product model in prisma/schema.prisma. Ran `bun run db:push` successfully. Restarted dev server to pick up new Prisma client.

Stage Summary:
- ✅ A1: Gon description restored in DB
- ✅ A2: Products section now has honeycomb-texture bg image with gradient overlay
- ✅ B3: StatCards no longer truncate large numbers
- ✅ B9: Logout redirects use relative URLs (no more localhost:3000)
- ✅ C17: Agent orders no longer show bonus hint
- ✅ C18: Agent sidebar "سفارش جدید" no longer also activates "تاریخچه سفارشات"
- ✅ C19: City auto-loads from agent profile in new order page (Radix Select workaround)
- ✅ Schema: Product model now has stockKg + agentPricePerKg for inventory + dual pricing
- Lint: PASS
- Remaining tasks (B4, B5, B6, B7, B8, B10, B11, B12, B13, B14, B15, C16) to be handled by subagents in parallel.

---
Task ID: B4-B5
Agent: full-stack-developer
Task: Agent order history — expandable rows with full item details + table alignment

Work Log:
- Read worklog.md and the existing `/admin/agents/[id]/page.tsx` to understand prior work (P5b-admin-panel-ui built the page; phase1-quick-fixes built the quick fixes).
- Confirmed Prisma schema: Order has `items: OrderItem[]`; OrderItem has productName, containerSize, hasWax, isWholesale, quantity, unitPrice, total (NO createdAt field on OrderItem — adjusted the include accordingly).
- Updated the Prisma query in `src/app/admin/(panel)/agents/[id]/page.tsx` `orders.select` clause to include `items: { select: { id, productId, productName, containerSize, hasWax, isWholesale, quantity, unitPrice, total } }`. The payments include block is untouched.
- Created new client component `src/components/admin/AgentOrderHistoryTable.tsx`:
  • `"use client"` directive.
  • Exports `AgentOrderWithItems` + `AgentOrderItem` types so the parent (server component) can pass the Prisma-shaped orders directly with proper typing.
  • Uses a `Set<string>` of order IDs for expand-state, toggled via a stable `useCallback`. Multiple rows can be expanded independently.
  • Renders the order history table: columns شماره سفارش، تاریخ، مبلغ، پرداخت، وضعیت، جزئیات.
  • Each row's order-number cell is a `Link` to `/admin/orders/${o.id}` with an `ExternalLink` icon (since the order-detail page is being built by another agent in parallel, we just link to it).
  • The «جزئیات» column has a chevron Button (ChevronDown / ChevronUp) that also shows the per-order item count (e.g. "۳ قلم") for at-a-glance context.
  • When expanded, a sub-row (`colSpan={6}`) renders an inner bordered Table listing every OrderItem with columns: محصول (with a Package icon + a "عمده" badge when isWholesale), ظرف (e.g. "۲ کیلو"), موم (با موم / بدون موم badge with Droplet icon), تعداد, قیمت واحد, جمع قلم.
  • Below the items sub-table there is a footer row showing total item count + total quantity, plus an outline "مشاهده صفحه کامل سفارش" button linking to `/admin/orders/${o.id}`.
  • Empty state: when `orders.length === 0`, renders the same `ShoppingCart` empty-state as before (preserved UX).
- Replaced the inline order-history Table JSX in the page (the old block with TableHeader / map / etc.) with `<AgentOrderHistoryTable orders={agent.orders} />`. Updated the CardDescription to add a hint «برای مشاهده جزئیات اقلام هر سفارش روی دکمهٔ «جزئیات» بزنید».
- Removed the now-unused imports `OrderStatusBadge`, `PaymentStatusBadge` from `@/components/agent/OrderStatusBadge` in the page (they live in the client component now). Added the import for `AgentOrderHistoryTable`.
- Kept the payment-history table below untouched (still uses the same shadcn Table primitives inline). The unused-import rule would have flagged `Table`, `Badge`, `formatJalaliDateTime` if they were unused — they are NOT (still used by the payment history), so they remain imported.
- Table alignment fix: explicit alignment classes applied to every header + cell.
  • Order-history table: text-right (شماره سفارش، تاریخ) · text-left (مبلغ — also `dir="ltr"` so the Persian currency string lays out correctly) · text-center (پرداخت، وضعیت، جزئیات badges/buttons).
  • Items sub-table: text-right (محصول) · text-center (ظرف، موم، تعداد) · text-left (قیمت واحد، جمع قلم — both `dir="ltr"`).
  • `whitespace-nowrap` added to date / amount / unit-price / line-total cells so they never wrap mid-number.
- RTL: parent already has `dir="rtl"`; the table inherits it. The Toman strings inside numeric cells use `dir="ltr"` so the "تومان" suffix stays to the right of the number rather than wrapping awkwardly.
- Verified lint: `bun run lint` → 0 errors / 0 warnings.
- Verified runtime: logged in as the seeded admin (`admin`/`admin12345`), fetched the agent `cmsyvxe200002s1g3z2jj7ruw` (محسن فرهیخته — has 2 orders). GET `/admin/agents/{id}` returned HTTP 200 in 767ms (compile 36ms + render 731ms). HTML contains: 2 "نمایش جزئیات سفارش" chevron buttons (one per order row, all collapsed), order numbers HN-81936 and HN-95548, the new «جزئیات» column header, and the «قلم» count label. Server-rendered HTML correctly does NOT include expanded item sub-tables (initial state is empty Set) — they only appear after client-side hydration + click.

Stage Summary:
- ✅ Prisma query now includes `items` per order (with all OrderItem fields needed for display).
- ✅ New client component `src/components/admin/AgentOrderHistoryTable.tsx` renders the order history with per-row expand/collapse using a `Set<string>` of order IDs.
- ✅ Expanded sub-row shows every OrderItem: product name, container size (kg), wax status, quantity, unit price (toman), line total — plus an "عمده" badge for wholesale items and totals (item count + total quantity) and a deep-link button to `/admin/orders/{id}`.
- ✅ Order number cell is a `Link` to `/admin/orders/${o.id}` (page being built by parallel agent — link is forward-compatible).
- ✅ Table column alignment fixed: text-right / text-center / text-left consistent across headers + cells; numeric cells use `dir="ltr"` so Persian Toman strings render cleanly.
- ✅ New «جزئیات» column with a chevron Button toggling row expansion; chevron icon swaps between ChevronDown / ChevronUp; `aria-expanded` + `aria-label` set for accessibility.
- ✅ Empty state preserved (ShoppingCart icon + "هنوز سفارشی ثبت نکرده است." message) when agent has no orders.
- ✅ Payment-history table below untouched.
- ✅ Lint PASS (0 errors, 0 warnings).
- ✅ Runtime verified: page returns HTTP 200 for a real agent with orders; renders both order rows + 2 expand buttons; SSR omits expanded sub-tables (correct).

---
Task ID: B6
Agent: full-stack-developer
Task: Admin order detail page with status management

Work Log:
- Read worklog.md to understand prior work (B4-B5 agent order history expandable rows; phase1-quick-fixes; agent-admin-panels-feature; bugfix-post-login-redirect).
- Read reference files: src/app/agent/(panel)/orders/[id]/page.tsx (status tracker pattern + payment/tracking cards), src/lib/format.ts (ORDER_STATUS_STEPS, statusStepIndex, formatToman, toPersianDigits, formatJalaliDateTime), src/components/agent/OrderStatusBadge.tsx (Badge components), src/lib/auth.ts (requireAdmin / getCurrentAdmin), prisma/schema.prisma (Order + OrderItem + Agent fields).
- Created src/app/api/admin/orders/[id]/route.ts — PATCH endpoint:
  • export const dynamic = "force-dynamic"
  • Calls requireAdmin() inside try/catch — converts the thrown UNAUTHORIZED error into a 401 JSON response (matches the pattern of existing admin APIs while honoring the task instruction to use requireAdmin).
  • Zod-validated body: { orderStatus?, trackingCode?, cancel? } with strict() so unknown fields are rejected.
  • Refuses to change an already-cancelled order (400 with clear Persian error message).
  • Honors the `cancel: true` shortcut → sets orderStatus to "cancelled" (regardless of current pipeline position).
  • Otherwise validates the requested status is forward-progress in ORDER_STATUS_STEPS — going backwards is blocked with "امکان بازگشت به وضعیت قبلی وجود ندارد" (except to cancelled, which is always allowed).
  • If transitioning to "shipped", requires a trackingCode (or one already stored on the order). If only a trackingCode is supplied without a status change, it's saved as an edit.
  • Returns the updated order (with items + agent relations) + a friendly Persian message per status.
- Created src/components/admin/AdminOrderDetail.tsx — client component:
  • Exports AdminOrder / AdminOrderItem / AdminOrderAgent types (plain JSON-serializable shapes; createdAt/updatedAt as ISO strings).
  • StatusTracker sub-component (ported from the agent page) — renders the 7-step horizontal timeline with the current step highlighted.
  • Status management card (the KEY feature):
    - Select for choosing next status. Available statuses computed via useMemo — backwards statuses are disabled (Rendered but greyed out / unselectable) so the admin sees the full pipeline but can't go backwards.
    - Input for tracking code (always visible, pre-filled when one already exists). When status is "shipped", shows a red asterisk + the apply button validates non-empty before submitting.
    - "اعمال وضعیت جدید" button — calls PATCH /api/admin/orders/[id] with { orderStatus, trackingCode? }. Shows a spinner (Loader2 animate-spin) while submitting. On success, calls window.location.reload() (per the bugfix-post-login-redirect pattern; more reliable than router.refresh()).
    - "لغو سفارش" red destructive button wrapped in an AlertDialog (shadcn alert-dialog) for confirmation. The dialog explains the order number + that the action is irreversible.
    - Sonner toast feedback on success and error.
  • Order header card: order number + CopyButton, customer/agent badge, creation date (Jalali), last-updated date (Jalali), final amount + Rial equivalent.
  • Customer/Agent info card: name, phone (tel: link), province, city, delivery type, address (if any). For agent orders: store name + a "صفحهٔ نماینده" button linking to /admin/agents/${agentId}.
  • Items table (shadcn Table): product name, container size (with "عمده" sub-label for wholesale), wax (دارد/ندارد), quantity, unit price, line total. Numeric cells use dir="ltr" + whitespace-nowrap.
  • Financial summary card: totalAmount, uniqueAmount, finalAmount, payment status badge.
  • Payment info card: when payment is pending → full card-number/holder/amount instructions (same as agent page). When payment is confirmed or order is cancelled → a smaller "پشتیبانی" card with the same info so the 2-column grid stays balanced.
  • Tracking info card: only rendered when shipped/delivered AND a trackingCode exists. Shows the code (with CopyButton) + a "پیگیری در سامانه پست" button linking to https://tracking.post.ir.
  • Notes card (dashed border) only when order.notes is set.
  • Footer actions: بازگشت به سفارش‌ها + (for agent orders) صفحهٔ نماینده.
  • RTL throughout, honey color palette (amber/honey/sky for tracking, red for destructive — no indigo/blue).
- Created src/app/admin/(panel)/orders/[id]/page.tsx — server component:
  • export const dynamic = "force-dynamic".
  • Double-checks getCurrentAdmin() (the (panel) layout already enforces auth via redirect, but the page does it too for defense-in-depth).
  • Fetches the order via db.order.findUnique with include: { items: true, agent: { select: { id, name, storeName, phone } } }.
  • notFound() if missing.
  • Maps the Prisma row → JSON-serializable shape (Date → .toISOString()) before passing to AdminOrderDetail. The agent's phone is fetched (for future use) but stripped from the serialized shape since AdminOrderDetail doesn't display it.
  • Renders <AdminOrderDetail order={serializableOrder} />.
- Updated src/app/admin/(panel)/orders/page.tsx — the "مشاهده" link in the orders table now points to `/admin/orders/${o.id}` (was `/track?orderNumber=${o.orderNumber}` with target="_blank"). Removed the target="_blank" + rel attributes since the admin should stay in the admin panel.
- Lint: bun run lint → 0 errors / 0 warnings.
- TypeScript: bunx tsc --noEmit → no errors in any of the new/modified files (other pre-existing errors in deploy/project/*, mini-services/telegram-bot, AddToCartDialog, Header, ProductCard, examples/*, skills/* are unrelated and were there before this task).
- Dev server log: no new errors after the changes (GET / 200s continue).

Stage Summary:
- ✅ New admin order detail route at /admin/orders/[id] — inside the protected admin (panel) layout, with force-dynamic.
- ✅ New PATCH API at /api/admin/orders/[id] — accepts { orderStatus?, trackingCode?, cancel? }, validates admin session via requireAdmin, blocks backwards transitions, requires tracking code when moving to "shipped".
- ✅ AdminOrderDetail client component delivers the full admin experience: status tracker, status management card (Select + tracking input + apply + cancel-with-confirm), customer/agent info, items table, financial summary, payment/tracking/notes cards.
- ✅ Admin orders list "مشاهده" link now stays inside the admin panel (no more /track?orderNumber=… in a new tab).
- ✅ Toast feedback (sonner) on success/error; window.location.reload() after success (reliable refresh pattern).
- ✅ RTL layout, honey palette, all existing shadcn/ui components (Card, Button, Badge, Select, Input, Label, AlertDialog, Table), Lucide icons.
- ✅ Lint clean, no type errors in new files.
- Files: 3 new (route.ts, page.tsx, AdminOrderDetail.tsx) + 1 modified (orders/page.tsx).

---
Task ID: B7-B8
Agent: full-stack-developer
Task: Reports PDF export + best-selling period filter

Work Log:
- Read worklog.md to understand prior work (image-gen, admin panel, stats already in place).
- Invoked `Skill(command="pdf")` to read the PDF skill instructions — but the project's API route environment doesn't ship with Playwright/puppeteer, so I went with a pure-JS server-side approach using `pdf-lib` + `@pdf-lib/fontkit` (installed via `bun add`) and an embedded Vazirmatn TTF.
- Downloaded Vazirmatn Regular/Medium/Bold TTFs from jsdelivr into `public/fonts/`.
- Wrote `src/lib/pdf/persian-shaping.ts` — a self-contained Persian/Arabic text shaper that (1) maps each Persian letter to its contextual presentation form (isolated/initial/medial/final) using the Unicode Arabic Presentation Forms-A/B blocks, (2) tokenizes mixed text into digit-runs vs Arabic-runs and reverses the segment order + reverses chars within non-digit segments, so that pdf-lib's LTR `drawText` produces the correct visual RTL layout including numbers/dates which stay in their natural LTR order.
- Modified `src/lib/stats.ts`:
  - Added `ReportPeriod` type export.
  - Extended `AdminStats` interface with `period`, `periodLabel`, `periodOrders`, `periodRangeStart`, `periodRangeEnd`, `weeklySeriesLabel`, `monthlySeriesLabel`.
  - Modified `computeAdminStats(opts?: { period?: "weekly" | "monthly" | "yearly" })` — period filters totalRevenue/agentRevenue/customerRevenue/topProducts/topAgents/orderStatusDistribution. Snapshots (thisWeekRevenue, thisMonthRevenue, growthPct, totalCommissionPaid, totalOrders, recentAgents/Orders) stay lifetime. weeklyRevenueSeries/monthlyRevenueSeries adapt per period (weekly: daily 7-day + 8-week series; monthly: 8-week + 6-month series; yearly: 12-month + 4-quarter series). Labels also adjust (e.g. "روند درآمد روزانه (۷ روز اخیر)").
- Wrote `src/lib/pdf/reports-pdf.ts` — the PDF builder. Uses honey palette (amber-600/800/100 backgrounds, no indigo/blue). Layout: top honey-background header bar with "سرزمین عسل" brand right-aligned + "گزارش فروش و تحلیل‌ها" subtitle + Jalali generation date on the left; title block "گزارش فروش <period>" + period range; 2×2 summary stat cards (period revenue, total orders lifetime, active agents out of total, total commission); top-products table (4 columns); top-agents table (5 columns); order-status distribution summary card; pagination via `ensureSpace()`; footer on every page with "صفحه X از Y" on right and "سرزمین عسل | گزارش فروش" on left.
- Created `src/app/api/admin/reports/pdf/route.ts` — `force-dynamic`, requires admin session, parses `?period=`, calls `computeAdminStats` then `generateReportsPdf`, returns `application/pdf` with `Content-Disposition: attachment; filename="sarzemine-asal-report-<YYYYMMDD>.pdf"`.
- Created `src/components/admin/DownloadReportsPdfButton.tsx` — client button using `useSearchParams` to read the current period, fetches the PDF as a blob, downloads via an injected `<a download>` link, toast on success/error. Uses honey-gradient.
- Created `src/components/admin/ReportsPeriodFilter.tsx` — client segmented-control with 3 buttons (هفتگی / ماهیانه / سالیانه) that updates the `?period=` URL query param via `router.replace` (defaults omit the param to keep URL clean for monthly). Uses `useTransition` for pending state.
- Modified `src/app/admin/(panel)/reports/page.tsx` — `searchParams: Promise<{ period?: string }>`, parses period, passes to `computeAdminStats`, header now shows `({stats.periodLabel})` and the date range, top stat card title uses `درآمد ${stats.periodLabel}` and hint shows `X سفارش در دوره • Y سفارش کل`, top-products description shows `در دوره ${periodLabel.toLowerCase()}`. Added `<Suspense>` wrappers (required by Next.js for `useSearchParams` in client components rendered from a server component) around `<ReportsPeriodFilter />` and `<DownloadReportsPdfButton />`. PDF button placed next to CSV button.
- Modified `src/components/admin/ReportsCharts.tsx` — added `weeklySeriesLabel`, `monthlySeriesLabel`, `periodLabel` optional props; the weekly/monthly chart titles now reflect the period-aware label from stats (e.g. "روند درآمد روزانه (۷ روز اخیر)" for weekly); revenue-share, status-distribution, top-products, top-agents, and growth chart titles get a `(دوره X)` suffix.
- Fixed an initial compile error: `@pdf-lib/fontkit` exposes only a default export, so changed `import { fontkit }` → `import fontkit from`.
- Verified end-to-end: logged in as admin (`/api/auth/admin/login` with admin/admin12345), hit `/admin/reports`, `/admin/reports?period=yearly`, `/admin/reports?period=weekly`, and `/api/admin/reports/pdf?period=monthly|yearly|weekly` — all return HTTP 200, the PDF is a valid 1-page 27 KB PDF, `pdftotext` confirms Persian text is correctly shaped (e.g. "گزارش فروش" + "27 مرداد 1405" date).
- `bun run lint` exits 0 (no errors or warnings).

Stage Summary:
- B7 (PDF export): server-side API route at `/api/admin/reports/pdf` generates a professional, honey-themed, RTL Persian PDF using pdf-lib + Vazirmatn font with manual Arabic shaping. Client "خروجی PDF" button on the reports page triggers the download. Verified for all 3 periods.
- B8 (period filter): `computeAdminStats({ period })` now accepts weekly/monthly/yearly; top products, top agents, order status distribution, revenue totals all scoped to the selected period; weekly/monthly series adapt granularity (daily/weekly/monthly/quarterly). New `ReportsPeriodFilter` segmented-control client component updates the URL query param. Reports page header and `ReportsCharts` titles reflect the active period.
- Files created: `src/lib/pdf/persian-shaping.ts`, `src/lib/pdf/reports-pdf.ts`, `src/app/api/admin/reports/pdf/route.ts`, `src/components/admin/DownloadReportsPdfButton.tsx`, `src/components/admin/ReportsPeriodFilter.tsx`, plus 3 Vazirmatn TTFs in `public/fonts/`.
- Files modified: `src/lib/stats.ts`, `src/app/admin/(panel)/reports/page.tsx`, `src/components/admin/ReportsCharts.tsx`.
- Dependencies added: `pdf-lib`, `@pdf-lib/fontkit`.

---
Task ID: B15
Agent: full-stack-developer
Task: Admin profile editing + password change

Work Log:
- Read worklog.md, src/lib/auth.ts, src/lib/format.ts, prisma/schema.prisma, and the analogous agent implementation (src/app/agent/(panel)/profile/page.tsx + src/components/agent/ProfileForm.tsx + src/app/api/agent/profile/route.ts) to understand patterns.
- Created `src/app/api/admin/profile/route.ts` — `export const dynamic = "force-dynamic"`; `GET` uses `requireAdmin()` and returns sanitized admin record (no `passwordHash`); `PATCH` accepts `{ name? }` for name update OR `{ currentPassword, newPassword }` for password change. Uses Zod schema with refine() to require currentPassword when newPassword is set. On password change: verifies currentPassword via `verifyPassword()`, validates new password strength via `isStrongPassword()` (6+ chars, letter + digit), then rehashes with `hashPassword()`. Returns sanitized updated admin + Persian message. Catches UNAUTHORIZED errors thrown by `requireAdmin()` and returns 401 instead of crashing.
- Created `src/components/admin/AdminProfileForm.tsx` — client component (`"use client"`) with three sections:
  1. Account overview card (cream-gradient bg, 3×2 grid of icon tiles): username (read-only, LTR), role badge, active status, name, last login (Jalali), member since (Jalali). Footer line shows last update time + truncated admin id.
  2. Profile info form: read-only username input + editable name input (pre-filled, maxLength 80, min 3 chars). PATCHes `{ name }`. Calls `window.location.reload()` 600ms after success to refresh sidebar/header adminName.
  3. Password change form: current/new/confirm password inputs (type=password, autoComplete hints). Client-side validates via `isStrongPassword()` from `@/lib/auth` and matches new === confirm. PATCHes `{ currentPassword, newPassword }`. Clears fields and reloads on success.
  Uses honey color palette (amber/honey only, no indigo/blue), RTL `dir="rtl"`, sonner toast feedback, Loader2 spinner during async, Lucide icons (User, Lock, Save, KeyRound, Shield, Hash, CalendarClock, UserCheck, CheckCircle2).
- Created `src/app/admin/(panel)/profile/page.tsx` — server component, `export const dynamic = "force-dynamic"`. Calls `getCurrentAdmin()` (redirects to `/admin/login` if no session), fetches full admin record via Prisma, redirects if admin missing or inactive, then passes serialized ISO strings to `AdminProfileForm`.
- Updated `src/components/admin/AdminSidebar.tsx`: imported `User` from `lucide-react`; appended 5th nav item `{ href: "/admin/profile", label: "پروفایل", icon: User }` after "گزارش‌ها". The existing `isActive()` logic (`pathname === href || pathname.startsWith(href + "/")`) correctly highlights `/admin/profile` and does not interfere with the `/admin` special case.
- Ran `bun run lint` — passed cleanly with no errors or warnings.
- Verified dev log shows no compile/runtime errors from the new files.

Stage Summary:
- Admin profile page live at `/admin/profile` with full edit-name + change-password flow.
- API at `/api/admin/profile` (GET + PATCH) follows the existing admin API pattern (requireAdmin auth, sanitized response, Zod validation).
- Sidebar now exposes the profile link to all admins (desktop right-side nav + mobile sheet menu).
- All text/labels/dates are RTL Persian; Jalali dates use `formatJalaliDateTime`; numbers use `toPersianDigits`.
- Honey/amber color palette preserved; no indigo or blue anywhere in the new code.
- Password security enforced both client-side (`isStrongPassword`) and server-side (`isStrongPassword` re-check + bcrypt hash) — defense in depth.
- Username, role, and account status are read-only by design (username is the login identifier; role/status are admin-managed fields).
- Files created: `src/app/admin/(panel)/profile/page.tsx`, `src/components/admin/AdminProfileForm.tsx`, `src/app/api/admin/profile/route.ts`.
- Files modified: `src/components/admin/AdminSidebar.tsx` (added `User` import + 5th nav item).

---
Task ID: C16
Agent: full-stack-developer
Task: Fix agent dashboard charts + recent orders

Work Log:
- Read worklog.md, src/app/agent/(panel)/page.tsx, src/components/agent/DashboardCharts.tsx, src/lib/stats.ts (computeAgentStats + AgentStats interface), src/components/agent/OrderStatusBadge.tsx (OrderStatusBadge + PaymentStatusBadge), src/lib/format.ts, src/components/ui/table.tsx, and recent dev.log.
- Diagnosed the data-flow bug: stats.ts mapped the DB English status keys → Persian labels for `recentOrders.orderStatus` / `recentOrders.paymentStatus`, then the dashboard page did the inverse (Persian → English) before passing to the badge components. That round-trip was lossy and brittle — the `o.paymentStatus === "تأیید شده" ? "confirmed" : "pending"` check happened to compare against the wrong Persian label (the stats used "تأیید شده" for the `confirmed` order status label, but the payment status label used "در انتظار"; the dashboard's check still didn't match the actual values returned by stats for payment = "confirmed" → returned "تأیید شده", so it happened to work, but for the wrong reason — it would also have returned "pending" for any other truthy Persian string).

src/lib/stats.ts (computeAgentStats only — interface left untouched):
- `recentOrders.orderStatus` and `recentOrders.paymentStatus` now return the raw English keys from the DB (e.g. `"awaiting_payment"`, `"paid"`, `"confirmed"`, `"pending"`) so the badge components can map them to Persian themselves. Removed the Persian-label mapping.
- `orderStatusDistribution` now always covers all 7 statuses (with `count: 0` for missing ones). Renamed local label table to match the badge labels (paid → "پرداخت ثبت شد", confirmed → "تأیید مدیریت", shipped → "تحویل به پست", delivered → "تحویل داده شد", cancelled → "لغو شد") so the donut chart legend matches what users see in the orders table.
- `topProducts` now sorted by revenue (`b.total - a.total`) descending instead of quantity — gives the agent a more meaningful "top earners" view.

src/app/agent/(panel)/page.tsx:
- Removed the broken `o.paymentStatus === "تأیید شده" ? "confirmed" : "pending"` mapping — `<PaymentStatusBadge status={o.paymentStatus} />` now receives the English key directly.
- Removed the `reverseStatusLabel()` call — `<OrderStatusBadge status={o.orderStatus} />` now receives the English key directly.
- Deleted the now-unused `reverseStatusLabel()` helper at the bottom of the file.
- Fixed column alignment: text columns `text-right`, amount column `text-left`, badge columns `text-center`. Added `whitespace-nowrap` to date and amount cells (explicit even though shadcn Table already sets it). Added a subtle honey-tinted hover on rows (`hover:bg-amber-50/50 dark:hover:bg-amber-900/10`). Order-number cell confirmed as a link to `/agent/orders/${o.id}`.
- Empty-state preserved (ShoppingBag icon + CTA button) when `recentOrders.length === 0`.

src/components/agent/DashboardCharts.tsx (full rewrite, same props interface):
- Extended `HONEY_PALETTE` from 6 to 7 amber-shade stops (dark→light: amber-900→amber-200) so every donut status gets a distinct color, and so the same status always maps to the same color.
- Added a typed `ChartTooltip` (replaces the untyped `any` version) — `dir="rtl"`, inline color swatch + name, and a LTR `<b>` for the formatted value so digits/`%`/Toman suffix render in the correct order.
- Added `ChartEmptyState` component (Inbox icon + caption) and used it for ALL four charts when there's no data: weekly line chart (`weeklyTotal === 0`), monthly bar chart (`monthlyTotal === 0`), donut (`totalOrdersInDist === 0`), and top products (`topProducts.length === 0`). Previously only the donut + top-products had empty states — the line + bar charts would render an empty axis with no data.
- Donut chart: filters out zero-count slices (so visually the donut only shows statuses that actually have orders), but the underlying data still includes all 7 — meaning the legend stays consistent in size/order. Replaced recharts' built-in `<Legend>` with a custom 2-column RTL `<ul>` that shows the Persian status label, count, and percentage (e.g. "تأیید مدیریت  ۳ · ۲۱٪"). Color swatch per entry uses the same `STATUS_COLOR_INDEX` map as the slice fill, so legend and slice colors always match.
- Top products chart: switched the bar value from `quantity` to `total` (revenue) to match the new sort order in stats.ts; description updated to "بر اساس درآمد (تومان)"; tooltip uses `formatToman` for proper Persian-digit + "تومان" suffix. Each bar still gets a distinct Cell color from `HONEY_PALETTE`.
- Weekly line chart: added a `<linearGradient id="weeklyLineStroke">` (FCD34D → D97706) for the line stroke, larger `activeDot` with a white stroke ring for a more polished hover, and a faint yellow cursor line on the tooltip.
- Monthly bar chart: added a vertical `<linearGradient id="monthlyBarFill">` (FCD34D top → D97706 bottom) for the bars, and a soft cream cursor-fill on the tooltip.
- Added explicit `width={48}` (line/monthly YAxis) and `width={92}` (top-products YAxis) so axis labels don't get truncated on mobile, and `dy={4}` on every XAxis for nicer vertical spacing.
- All chart containers keep `dir="ltr"` so recharts renders correctly; tooltips explicitly `dir="rtl"`.
- Removed unused `Legend` import (no longer needed since the donut has a custom legend) and the now-unused `productColors` gradient def on the top-products bar (the Cell fills override the bar fill anyway, so the def was dead code).

Verification:
- `bun run lint` → 0 errors / 0 warnings.
- `bunx tsc --noEmit` → no new errors in any of the 3 modified files. (Pre-existing errors in `src/app/admin/(panel)/page.tsx:77` fallback AdminStats object — missing period fields from B7-B8 — and `src/app/api/agent/orders/route.ts:176` unrelated argument type — are not introduced by this task.)
- Dev log shows no compile/runtime errors after the changes; the agent dashboard route still serves 200s.

Stage Summary:
- ✅ No more broken reverse-mapping: DB English status keys flow straight from `computeAgentStats` → `recentOrders` → `<PaymentStatusBadge>` / `<OrderStatusBadge>`. The fragile Persian→English→Persian round-trip is gone.
- ✅ All four charts render correctly with proper data and have graceful empty states (no more empty axis when an agent has 0 sales in the window).
- ✅ Donut chart legend is a polished 2-column RTL grid with color swatch + label + count + percentage, color-stable per status across renders.
- ✅ Top products now sorted by revenue (Toman) and the bar chart shows revenue with Toman formatting — matches the description "بر اساس درآمد (تومان)".
- ✅ Recent orders table: aligned columns (text-right / text-left / text-center), `whitespace-nowrap` on date + amount, order-number is a link, subtle honey hover on rows, preserved empty-state CTA.
- ✅ Honey palette preserved (no indigo/blue), RTL throughout, all existing shadcn/ui components reused.
- ✅ AgentStats interface unchanged — only the values returned for `recentOrders.orderStatus`/`paymentStatus` changed (English keys instead of Persian labels), and `orderStatusDistribution` now always has all 7 entries.
- Files modified: src/lib/stats.ts (computeAgentStats section only), src/app/agent/(panel)/page.tsx (recent orders table + removed reverseStatusLabel helper), src/components/agent/DashboardCharts.tsx (full rewrite).

---
Task ID: B10-B11-B13-B14
Agent: full-stack-developer
Task: Inventory management + stock validation + product CRUD + dual pricing

Work Log:
- Read worklog.md (especially the phase1-quick-fixes entry which added `stockKg Float @default(0)` and `agentPricePerKg Int @default(0)` to the Product schema) plus src/lib/auth.ts, src/lib/format.ts, src/lib/products.ts, src/app/api/orders/route.ts, src/app/api/agent/orders/route.ts, src/app/api/products/route.ts, prisma/schema.prisma, src/app/agent/(panel)/orders/new/page.tsx, src/components/admin/AdminSidebar.tsx, src/components/admin/AdminProfileForm.tsx, src/components/admin/AgentActionsButtons.tsx, src/components/ui/{dialog,alert-dialog,switch,table,input}.tsx, src/lib/agent-cart-store.ts, src/lib/notify-bot.ts, src/components/site/{ProductCard,AddToCartDialog,ProductsView,HomeView,CartView}.tsx. Also confirmed the live SQLite pragmas and dev.log state.

- Created `src/app/api/admin/products/route.ts` (B13 list + create):
  • `export const dynamic = "force-dynamic"` + `requireAdmin()`-style auth (uses `getCurrentAdmin()` returning 401 JSON on no session — matches the existing pattern in `/api/admin/agents/route.ts`).
  • `GET`: lists all products ordered by `[featured desc, createdAt asc]`. Returns the full row incl. `stockKg` and `agentPricePerKg` (admin-only).
  • `POST`: Zod-validated body (name ≥2, slug regex `^[a-z0-9-]+$`, description ≥5, pricePerKg integer ≥0, agentPricePerKg integer ≥0 default 0, stockKg number ≥0 default 0, color/origin/benefits strings, image nullable, featured boolean). Enforces slug uniqueness with a separate `findUnique` before insert. Creates the product and returns `{ success, product, message }`.

- Created `src/app/api/admin/products/[id]/route.ts` (B13 fetch/edit/delete):
  • `export const dynamic = "force-dynamic"`. Same auth pattern.
  • `GET`: fetches a single product by id; 404 if missing.
  • `PATCH`: Zod-validated partial schema (all fields optional; `agentPricePerKg` and `stockKg` allow fractional stock updates). Performs an explicit slug uniqueness check when slug changes. Returns the updated product + Persian message. This same endpoint serves the inline `StockEditor` (B10 quick-update stock-only).
  • `DELETE`: 404 if missing; refuses hard-delete if the product is referenced in any `OrderItem` (counts via `db.orderItem.count({ where: { productId: id } })`). Returns a clear Persian error recommending "set stock to 0 or un-feature instead". When safe, hard-deletes the row.

- Created `src/components/admin/ProductManager.tsx` (B10 + B13 UI, ~1270 lines, fully client):
  • Exports `AdminProduct` interface (Prisma-shaped, Date → ISO strings) so the server page can pass products straight through.
  • Helpers: `toEnglishDigits` (Persian + Arabic → ASCII), `parseNumberInput` (parses Persian-style numeric input), `slugify` (auto-suggest slug from Persian name → ASCII).
  • `ProductFormDialog` sub-component: shared create/edit dialog. State for all 11 fields + `slugTouched` (so we don't auto-overwrite a user-edited slug) + `imgBusy`/`imgError` for the file input. Reads file via `FileReader.readAsDataURL()` and stores the resulting data URL in the form state (B13 sandbox image approach). 1.5MB cap with friendly Persian error. "پاک کردن عکس" button sets `image: null` and clears the file input. Live formatting preview of the customer/agent prices via `formatToman`. Client-side validation mirrors the server Zod (name ≥2, slug regex, pricePerKg integer ≥0, stockKg number ≥0). Submits POST for create / PATCH for edit, then `router.refresh()` after a short delay.
  • `StockEditor` sub-component (B10): inline LTR input + "کیلو" label + a Save icon button that lights up only when dirty. Live-renders `text-red-600 border-red-300` when value < 5 (LOW_STOCK_THRESHOLD). PATCHes just `{ stockKg }` and calls `onSaved` (which triggers `router.refresh()`).
  • `DeleteDialog` sub-component (B13): uses shadcn AlertDialog for confirm-with-explanation. Calls DELETE on the API; surfaces server errors (e.g. "product is referenced in N orders") via sonner toast.
  • Main `ProductManager`: toolbar (search + "افزودن محصول جدید" button) + 4 quick-stat tiles (total, featured, low-stock, out-of-stock — colored amber/red when non-zero) + search input + responsive product table (desktop shadcn Table; mobile card-list). For each product row: image thumbnail (or honey-tinted Package icon placeholder), name + origin, slug (LTR), customer price (bold), agent price (honey-dark bold if set, "همان مشتری" muted italic if 0), inline `StockEditor`, featured badge, edit + delete icon buttons. Last-updated footer with Jalali timestamp.
  • All icons from lucide-react (Package, Plus, Pencil, Trash2, Save, AlertTriangle, Image, Loader2, Search, Star, X, Boxes, TrendingDown). Honey palette only (no indigo/blue). Full RTL via `dir="rtl"`.

- Created `src/app/admin/(panel)/products/page.tsx` (server component):
  • `export const dynamic = "force-dynamic"`. Calls `getCurrentAdmin()` → redirects to `/admin/login` if no session (defense-in-depth on top of the (panel) layout's auth).
  • Fetches all products ordered by `[featured desc, createdAt asc]`, maps Date → ISO string, and hands the JSON-serializable array to `<ProductManager products={...} />`.

- Updated `src/components/admin/AdminSidebar.tsx` (B10/B13 nav):
  • Imported `Package` from lucide-react.
  • Added a new nav item `{ href: "/admin/products", label: "محصولات", icon: Package }` as the 2nd item (right after "داشبورد") so it's prominent. Existing `isActive()` logic correctly handles it via `pathname === href || pathname.startsWith(href + "/")` (no interference with `/admin` exact-match special case).

- Updated `src/app/api/products/route.ts` (B14 + B11):
  • Now uses `select` to return a curated subset: `id, name, slug, description, benefits, pricePerKg, agentPricePerKg, color, origin, image, featured, createdAt, updatedAt`. **Critically omits `stockKg`** — that field is never exposed to customers or agents (per B11 instructions).
  • Ordering changed to `[featured desc, createdAt asc]` so featured products surface first.
  • Kept all existing no-cache headers + dynamic/revalidate/fetchCache directives.

- Updated `src/app/api/orders/route.ts` (B11 customer-side):
  • `dbProducts.findMany` select now includes `stockKg` (so we can validate without leaking to the client).
  • After per-item price revalidation, aggregates `requestedKgByProduct` (productId → total kg). For each product, if `requestedKg > stockKg + 0.001` returns HTTP 400 with the exact Persian error specified in the task: `موجودی کافی نیست: {productName} — موجودی فعلی: {stockKg} کیلو، درخواست شما: {requestedKg} کیلو. لطفاً سفارش خود را تنظیم کنید.` (digits in Persian via `toPersianDigits`; trailing zeros stripped for cleaner display).
  • Wraps the order insert + all stock decrements in `db.$transaction(async (tx) => ...)` so the order is never created without stock decrements (and vice versa).
  • Imported `toPersianDigits` from `@/lib/format`.

- Updated `src/app/api/agent/orders/route.ts` (B11 + B14 agent-side):
  • `dbProducts.findMany` select now includes `agentPricePerKg` and `stockKg`.
  • **B14 dual pricing**: computes `effectivePricePerKg = p.agentPricePerKg > 0 ? p.agentPricePerKg : p.pricePerKg` and uses it for the `unitPrice` (`Math.round(effectivePricePerKg * containerSize)`). Agents now pay the agent-specific price when set, otherwise the customer price.
  • **B11 stock validation**: same aggregation + 400-with-Persian-message pattern as the customer flow.
  • Wraps order creation + stock decrements in `db.$transaction`. Imported `toPersianDigits`.

- Updated `src/app/agent/(panel)/orders/new/page.tsx` (B14 agent UI):
  • Added `agentPricePerKg?: number` to the local `Product` interface.
  • In the product list render: derives `hasAgentPrice` and `effectivePricePerKg` (agent price when set >0 and ≠ customer price, else customer price). Shows the agent price in honey-dark bold + the customer price in strikethrough muted when there's a distinct agent price; otherwise shows just "قیمت هر کیلو".
  • Updated `unitPrice = containerPrice(effectivePricePerKg, sel.size)` and `handleAddToCart` to use the effective price for both display and the `pricePerKg` field of the cart item. (Server still re-computes the unitPrice from the live DB — defense in depth.)

- Fixed pre-existing `notifyBotNewOrder` type error: changed the signature from `(orderNumber: string)` to `(payload: string | { orderNumber: string; [k: string]: unknown })` so the agent flow's rich-object call type-checks. The bot service only reads `body.orderNumber` at runtime so behavior is unchanged. Removed an earlier "spread after literal key" lint error by using `...payload` alone (not `{ orderNumber: payload.orderNumber, ...payload }`).

- Lint: `bun run lint` → 0 errors, 0 warnings. Cleaned up 3 unused `eslint-disable @next/next/no-img-element` directives in `ProductManager.tsx` (the eslint config doesn't flag `<img>` in this project so the directives were dead).

- TypeScript: `bunx tsc --noEmit` → no errors in any of the files I created/modified (`src/app/admin/(panel)/products/page.tsx`, `src/components/admin/ProductManager.tsx`, `src/app/api/admin/products/route.ts`, `src/app/api/admin/products/[id]/route.ts`, `src/app/api/products/route.ts`, `src/app/api/orders/route.ts`, `src/app/api/agent/orders/route.ts`, `src/lib/notify-bot.ts`, `src/components/admin/AdminSidebar.tsx`, `src/app/agent/(panel)/orders/new/page.tsx`). All remaining tsc errors in the repo are pre-existing in unrelated files (deploy/, mini-services/telegram-bot, examples/, skills/, AddToCartDialog `string | null` issue, Header `cart` nav-key issue, AdminStats period fallback, reports-pdf Uint8Array → BodyInit).

Stage Summary:
- ✅ B10 (Inventory): admin Products page at `/admin/products` with a full management table + inline `StockEditor` that PATCHes `{ stockKg }` per row. Low-stock (<5kg) indicator (red text + amber stat tile). Quick stats show total/featured/low-stock/out-of-stock counts.
- ✅ B11 (Stock validation): both `/api/orders` (customer) and `/api/agent/orders` (agent) validate each item's `containerSize * quantity` against `p.stockKg` before creating the order, returning the exact Persian error message requested. Stock is decremented atomically inside `db.$transaction` so the order is never persisted without the inventory decrement (and vice versa). Stock is NEVER exposed via the public `/api/products` route (only via the admin-only `/api/admin/products`).
- ✅ B13 (Product CRUD): admin can create/edit/delete products via a single dialog handling all fields (name, auto-suggested slug, description, benefits, pricePerKg, agentPricePerKg, stockKg, color, origin, featured, image upload via data URL). DELETE is blocked when the product is referenced in any OrderItem (returns a clear Persian error suggesting to set stock=0 / un-feature instead). Image upload uses `FileReader.readAsDataURL()` → stored as data URL in `image` field; 1.5MB cap with friendly error. "پاک کردن عکس" button to clear the image. Placeholder honey-tinted Package icon shown when no image.
- ✅ B14 (Dual pricing): `/api/agent/orders` POST computes `unitPrice = Math.round((p.agentPricePerKg > 0 ? p.agentPricePerKg : p.pricePerKg) * containerSize)`. Agent new-order page displays the agent price (when set) with the customer price in strikethrough; uses the effective price in the cart add + cart item storage. Server still re-authoritatively recomputes the unit price from the DB on order submission.
- ✅ AdminSidebar nav: "محصولات" item added in the 2nd slot with Package icon.
- ✅ Public `/api/products` route: returns `pricePerKg`, `agentPricePerKg`, but NOT `stockKg`.
- ✅ Admin API: `/api/admin/products` (GET list, POST create) + `/api/admin/products/[id]` (GET, PATCH, DELETE).
- ✅ Fixed pre-existing `notifyBotNewOrder` type error (now accepts `string | object`).
- ✅ Lint PASS (0 errors / 0 warnings). TypeScript PASS for all new/modified files.
- ✅ Honey palette (no indigo/blue), RTL throughout, shadcn/ui components reused (Card, Button, Input, Label, Textarea, Switch, Badge, Table, Dialog, AlertDialog), Lucide icons.
- Files created: `src/app/api/admin/products/route.ts`, `src/app/api/admin/products/[id]/route.ts`, `src/components/admin/ProductManager.tsx`, `src/app/admin/(panel)/products/page.tsx`.
- Files modified: `src/components/admin/AdminSidebar.tsx` (added `Package` import + nav item), `src/app/api/products/route.ts` (curated select), `src/app/api/orders/route.ts` (B11 validation + transaction decrement), `src/app/api/agent/orders/route.ts` (B14 dual pricing + B11 validation + transaction decrement), `src/app/agent/(panel)/orders/new/page.tsx` (show agent price + use effective price), `src/lib/notify-bot.ts` (accept string|object payload).